<UserControl x:Class="Drive_Smart_2._0.UserControls.SideBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Background="Transparent">

        <StackPanel Margin="10">

            <TextBlock Text="RENTAL SYSTEM"
                       FontSize="21"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,20,0,30"/>

            <Button x:Name="BtnHome"
                    Content="Home"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnDashboard"
                    Content="Dashboard"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnVehicles"
                    Content="Vehicles"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnCustomers"
                    Content="Customers"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnBookings"
                    Content="Bookings"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnPayments"
                    Content="Payments"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnReports"
                    Content="Reports"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            <Button x:Name="BtnEmployees"
                    Content="Employees"
                    Background="#1565C0"
                    Style="{StaticResource SidebarButtonStyle}"/>

            

        </StackPanel>

    </Grid>

</UserControl>